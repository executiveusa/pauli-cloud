# ZTE-20260716-0003 — P1 Enterprise Safety Hotfix

## Objective

Correct the three P1 defects merged in version 1.0.0 without expanding product scope.

## Risk

MEDIUM. Changes affect policy installation rollback, approval enforcement, and verification-before-commit gates. Production publication remains prohibited.

## Changes

- preserve the merged policy, runtime, and integrity modules byte-for-byte as legacy modules;
- introduce narrow audited wrappers at the existing public module paths;
- preflight all uninstall drift and backup requirements before mutation;
- maintain a rollback journal for unexpected uninstall failures;
- bind enterprise verification evidence to the current bead, phase, and phase-start audit event;
- enforce exact or project approval scope inside generated Claude hooks;
- add black-box regression tests for all three defects;
- bump package metadata to 1.0.1.

## Binary acceptance

- uninstall drift produces zero managed-file mutations and preserves the manifest;
- stale or mismatched evidence cannot enter COMMIT through runtime or policy guard;
- current phase-bound evidence permits COMMIT;
- exact scope permits only the named protected file;
- project scope permits all protected files;
- Node 20/22, coverage, secret scan, enterprise verification, package, container, CodeQL, and dependency review gates pass.

## Rollback

Revert the hotfix commit. The original merged modules remain preserved as `*-legacy.mjs` for forensic comparison, but production release remains blocked until the hotfix is merged and issue #5 is complete.
